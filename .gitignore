# Local Claude Code settings (machine-specific paths, not useful to anyone else).
# .claude/launch.json IS committed - it tells the dev server how to start.
.claude/settings.local.json

.DS_Store

# This project lives in Dropbox, which forks a copy whenever it and a build tool
# write the same file (Vite's dep cache, mostly).
*conflicted copy*
